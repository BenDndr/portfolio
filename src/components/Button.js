import '../style/Button.css'

const Button = ({buttonText}) => {
    
    return (
        <div>
            <button>
                    {buttonText}
            </button>
        </div>
    )
}

export default Button
